apiVersion: v1
kind: Pod
metadata:
  labels:
    run: result-app-pod
    app: demo-voting-app
  name: result-app-pod
spec:
  containers:
  - name: result-app
    image: kodekloud/examplevotingapp_result:v1
    ports:
    - containerPort: 80
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
